[package]
name = "wasm-primes"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]  # Required for WASM compilation

[dependencies]
wasm-bindgen = "0.2"   # For JS-Rust interoperability
js-sys = "0.3"         # For converting Rust types to JS
